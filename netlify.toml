[build]
  command = "npm run build"
  publish = "dist"

# Prevent Netlify CDN from caching API responses
[[headers]]
  for = "/api/*"
  [headers.values]
    Cache-Control = "no-store, no-cache, must-revalidate"

# Proxy /api/* to Hostinger backend (via direct IP) — avoids CORS entirely
[[redirects]]
  from = "/api/*"
  to = "https://145.79.213.33/api/:splat"
  status = 200
  force = true

# SPA fallback — must be last
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
