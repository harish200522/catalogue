[build]
  command = "npm run build"
  publish = "dist"

# Proxy /api/* to Render backend — avoids CORS entirely
[[redirects]]
  from = "/api/*"
  to = "https://catalogue-26cn.onrender.com/api/:splat"
  status = 200
  force = true

# SPA fallback — must be last
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
